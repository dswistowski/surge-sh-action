{
  "name": "chartscii",
  "version": "4.0.3",
  "description": "awesome ascii charts",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tool3/chartscii"
  },
  "files": [
    "./dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "print": "node -e \"const keys = require('./snapshots/index.test'); Object.keys(keys).map(key => console.log(key + '\\n' + keys[key].join('\\n'))) \" ",
    "print:tests": "node -e \"const fs = require('fs'); const path = require('path'); const dir = './tests/snapshots'; fs.readdirSync(dir).filter(f => f.endsWith('.json')).forEach(file => { console.log('\\n=== ' + file + ' ===\\n'); const data = require(path.join(process.cwd(), dir, file)); Object.keys(data).forEach(key => console.log(key + '\\n' + data[key].join('\\n'))); });\"",
    "print:test": "node -e \"const name = process.argv[1]; const data = require('./tests/snapshots/' + name + '.test.json'); Object.keys(data).forEach(key => console.log(key + '\\n' + data[key].join('\\n')));\" --"
  },
  "keywords": [
    "bar-chart",
    "cli",
    "ascii",
    "chart"
  ],
  "author": "Tal Hayut",
  "license": "MIT",
  "sideEffects": false,
  "devDependencies": {
    "@types/node": "^25.6.0",
    "dvd": "file:../dvd",
    "shellfie": "^2.4.4",
    "snaptdout": "^2.1.0",
    "ts-node": "10.9.2",
    "tsup": "^8.5.1",
    "typescript": "5.4.5",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "styl3": "3.1.1"
  }
}
