{
  "author": "Isaac Z. Schlueter <i@izs.me> (http://blog.izs.me/)",
  "name": "surge-fstream-ignore",
  "description": "A thing for ignoring files based on globs",
  "version": "1.1.0",
  "repository": {
    "type": "git",
    "url": "git://github.com/sintaxi/surge-fstream-ignore.git"
  },
  "main": "ignore.js",
  "scripts": {
    "test": "tap test/*.js --cov",
    "preversion": "npm test",
    "version": "npm publish",
    "postversion": "git push --no-verify && git push --tags --no-verify && git push --no-verify"
  },
  "dependencies": {
    "fstream": "1.0.12",
    "inherits": "2.0.4",
    "minimatch": "7.4.6"
  },
  "devDependencies": {
    "mkdirp": "0.5.6",
    "rimraf": "3.0.2",
    "tap": "^5.7.1"
  },
  "license": "ISC",
  "files": [
    "ignore.js"
  ]
}
